# ذاكرة المشروع — تعليمات دائمة لـ Claude

> هذا الملف يُقرأ تلقائياً في بداية كل جلسة. التزم به دائماً.

## 👤 من هو صاحب المشروع
- **محاسب محترف، وليس مبرمجاً.** اشرح كل شيء بلغة بسيطة وبالعربية، وبأمثلة من عالم
  المحاسبة عند الإمكان. أي مصطلح تقني لازم تشرحه بكلمة أو جملة بسيطة بين قوسين.
- لا تُغرقه في تفاصيل برمجية إلا لو طلب. ركّز على: «إيه اللي اتعمل»، «إيه اللي ينفع
  يعمله النظام»، و«إيه المطلوب منه هو».

## 🧭 أسلوب اتخاذ القرار (مهم)
- **اتّخذ كل القرارات التقنية والأمنية والمعمارية بنفسك، بدون ما تسأله**، واختر دائماً
  الأفضل من ناحية: (1) الأمان، (2) الاستدامة على المدى الطويل، (3) سهولة الصيانة.
- اسأله **فقط** في القرارات اللي تخص **منطق العمل المحاسبي** أو رغبته الشخصية في شكل
  النظام (مثال: عايز تقرير كذا يطلع إزاي) — مش القرارات التقنية.
- لو في اختيار تقني، خده من نفسك واذكر باختصار إنك اخترته وليه.

## 📦 المشروع
- نظام **ERP محاسبي عام شامل**، SaaS، عربي/إنجليزي، موجّه للشركات ومكاتب المحاماة.
- **المرجع الكامل للمتطلبات:** `docs/ERP_MASTER_PROMPT_AR.md` (البرومبت النهائي الموحّد).
- **مكان الكود:** مجلد `erp/` (الـ `backend` و`frontend`). أما `backend/` و`frontend/`
  في جذر الريبو فدول نظام مدرسة قديم منفصل — لا علاقة له بالـ ERP.
- **التقنيات المثبّتة:** Laravel + React + MySQL + Docker + Sanctum.
  تعدد مستأجرين بقاعدة واحدة + `company_id`. العملة KWD (الدينار الكويتي، 3 خانات).
  الضريبة معطّلة افتراضياً وقابلة للتفعيل.

## 🔁 طريقة العمل
- ابنِ **مرحلة بمرحلة** حسب الخطة في ملف البرومبت. لكل مرحلة: تنفيذ → اختبارات تمرّ
  100% → commit وصفي → تقرير بسيط لصاحب المشروع.
- **التسليم بدون أخطاء**: لا تقل «تمّ» إلا بعد ما الاختبارات تنجح فعلاً.
- **النسخ الاحتياطي:** لو الـ push على GitHub مرفوض، ابعت للمستخدم ملف zip بالسورس بعد
  كل مرحلة (البيئة مؤقتة والشغل ممكن يضيع).

## 📖 قاموس مصطلحات (بالبسيط)
- **المستأجر (Tenant):** كل شركة/عميل بيستخدم النظام. زي عمارة فيها شقق: النظام =
  العمارة، وكل شركة = شقة (مستأجر) ليها بياناتها لوحدها ومحدش يقدر يبصّ في شقة غيره.
  «تعدد المستأجرين» = نظام واحد بيخدم شركات كتير، كل شركة تشوف بياناتها هي بس.
- **Backend (الخلفية):** «محرّك» النظام اللي بيشتغل على السيرفر (الحسابات، القواعد،
  الأمان). المستخدم مايشوفهوش.
- **Frontend (الواجهة):** الشاشات اللي المستخدم بيشوفها ويضغط عليها.
- **API:** «خط التواصل» بين الواجهة والمحرّك.
- **Token (التوكن):** «تذكرة دخول» مؤقتة بتثبت إن المستخدم مسجّل دخوله.
- **Commit / Push:** «حفظ نسخة» من الشغل / «رفعها» على GitHub (الأرشيف السحابي).
- **Migration:** «وصفة» بتبني جداول قاعدة البيانات تلقائياً.
- **RBAC (الصلاحيات):** تحديد مين يقدر يعمل إيه (يشوف/يضيف/يعتمد/يحذف...) حسب دوره.
